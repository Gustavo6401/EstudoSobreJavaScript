const PI = 3.14; /// Variável constante (somente leitura).

// As variáveis em js começam com o valor undefined.
var x; // Valor - undefined
x = 10; // Valor inteiro32 (x = 10)

// Em JavaScript, um int pode virar uma string.
x = "Você é: " + x + "!"; // "Você é 10!"

var gostoso = true; // Boolean
gostoso; // Variável global

if(gostoso) {
    let gustavo; // Variável local
}

var vetor = [1, 2, 3]; // Vetor em JS.